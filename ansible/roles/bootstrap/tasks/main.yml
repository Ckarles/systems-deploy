---

- name: "Generate kickstart configuration"
  import_tasks: generate-kickstart.yml
  tags: ['kickstart']

- name: "Bootstrap target \"{{ inventory_hostname }}\""
  import_tasks: bootstrap.yml
  tags: ['bootstrap']
